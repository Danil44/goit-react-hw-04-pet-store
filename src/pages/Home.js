import React from 'react';
import styled from '@emotion/styled';

const Title = styled.h1`
  font-family: sans-serif;
  color: hsla(0, 0%, 18%, 0.91);
  text-align: center;
`;

const Home = () => {
  return <Title>Welcome to Paws online store</Title>;
};

export default Home;
